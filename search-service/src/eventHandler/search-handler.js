const logger = require('./utils/logger.js');

const handlePostCreated = async() => {
    logger.info('')
    try {

    } catch (error) {
        logger.error('')
    }
}

module.exports = { handlePostCreated };